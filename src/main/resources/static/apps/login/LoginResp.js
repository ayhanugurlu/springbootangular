/**
 * Login responce model
 * @param firstname
 * @param lastname
 * @param token
 * @constructor
 */
function LoginReq(firstName, lastName, token) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.token = token;
}
